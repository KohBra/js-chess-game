import Move from '../Move.js'
import { implement } from '../helpers.js'

export default class SpecialMove extends Move
{
    execute (board, methods) {
        implement('Implement execute()')
    }
}